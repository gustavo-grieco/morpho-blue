// SPDX-License-Identifier: GPL-2.0
pragma solidity ^0.8.0;

contract Clamped {
    function clampedDeployMarket(
        uint256 irmIndex,
        uint256 collateralIndex,
        uint256 loanIndex,
        uint256 lltvIndex,
        uint256 irm,
        uint256 lltv1,
        uint256 lltv2
    ) public {
        // deployTokenOracleIRM();
        // deployTokenOracleIRM();
        // deployTokenOracleIRM();
        // morpho_enableLltv(lltv1);
        // morpho_enableLltv(lltv2);
        // morpho_enableIrm(0);
        // morpho_deployMarket(0, collateralIndex, loanIndex, lltvIndex);
        // t(false, 'market');

        // function morpho_supplyAssetsClamped(uint256 assets) public {
        //     switchMarket(0);
        //     morpho_supplyAssets(assets);
        //     t(false, 'Quick supply canary');
        // }
    }
}
